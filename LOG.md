# Журнал разработки Astro Processor

## Проект

Локальный модульный астрологический процессор для личного использования.

## Долгосрочная цель

Создать профессиональный, модульный, точный и развиваемый астрологический продукт:

1. Натальные карты.
2. Аспекты.
3. Транзиты.
4. Прогрессии.
5. Интерпретации по базе знаний.
6. LLM-ассистент в строгом режиме.
7. Позже — дополнительные исследовательские модули.

---

## Stage 01 — Natal Chart Core (версия 0.2.0, модульная)

**Статус:** завершён.

### Что сделано

1. Создана модульная архитектура:
   - `astro_core/constants.py` — константы и настройки по умолчанию;
   - `astro_core/utils.py` — вспомогательные функции;
   - `astro_core/time_service.py` — время и Julian Day;
   - `astro_core/points.py` — Part of Fortune;
   - `astro_core/aspects.py` — расчёт аспектов;
   - `astro_core/ephemeris.py` — адаптер Swiss Ephemeris;
   - `astro_core/chart.py` — сборка натальной карты;
   - `run.py` — входной файл программы.

2. Подключена библиотека Swiss Ephemeris (`pyswisseph`).

3. Реализована поддержка:
   - тропического зодиака;
   - системы домов Placidus;
   - Mean/True лунных узлов;
   - Хирона;
   - Part of Fortune;
   - мажорных аспектов;
   - настраиваемых орбов.

4. Добавлены unit-тесты для ключевых модулей.

5. Создана проектная документация:
   - `README.md`
   - `LOG.md`
   - `requirements.txt`
   - `config/default_settings.json`

6. Проверено:
   - расчёт демо-карты;
   - подключение файлов эфемерид из папки `ephe`;
   - работа с Python 3.11.

### Ограничения Stage 01

- Часовой пояс и UTC offset вводятся вручную.
- Поиск города не реализован.
- Поддерживается только tropical zodiac.
- Part of Fortune использует упрощённую логику day/night.
- Нет веб-интерфейса.
- Нет транзитов и прогрессий.
- Нет интерпретаций.

### Риски, учтённые на этом этапе

- Python 3.14 не подошёл, установлен Python 3.11 рядом.
- Swiss Ephemeris требует корректной структуры возвращаемых данных (исправлена обработка кортежа).
- Разные версии pyswisseph имеют разные API для set_ephe_path (реализована универсальная проверка).

---

## Stage 02 — Transit Engine (версия 0.3.0)

**Статус:** завершён.

### Что сделано

1. Создан модуль `astro_core/transits.py`:
   - `calculate_transit_positions` — расчёт положений транзитных планет на дату;
   - `find_transits_on_date` — нахождение аспектов между транзитными и натальными точками;
   - `get_transit_calendar` — календарь транзитов за период (по дням).

2. Обновлён `run.py`:
   - добавлен режим `--transits`;
   - поддержка одиночной даты (`--transit-date`) и диапазона (`--start-date`, `--end-date`);
   - поддержка `--transit-days` для быстрого календаря;
   - вывод в читаемом формате и JSON (`--json-output`).

3. Добавлены тесты для транзитов:
   - расчёт транзитных позиций;
   - нахождение транзитов на дату;
   - календарь транзитов за период;
   - проверка корректности дат.

4. Обновлена документация:
   - README.md с примерами транзитов;
   - LOG.md с итогами Stage 02.

### Ограничения Stage 02

- Календарь транзитов по дням, без точных часов.
- Нет уточнения точных дат аспектов (вход в орб, пик, выход из орба).
- Нет специальной обработки ретроградных петель.
- Для больших периодов расчёт может быть медленным.

### Риски, учтённые на этом этапе

- Добавлены тесты для защиты от регрессий.
- Модуль транзитов изолирован от остального кода.
- Используется полдень как момент расчёта для календаря по дням.

---

## Stage 03 — Advanced Transits (версия 0.4.0)

**Статус:** завершён.

### Что сделано

1. Добавлены функции поиска точных дат аспектов в `astro_core/transits.py`:
   - `normalize_diff` — нормализация разницы углов;
   - `get_aspect_target_longitudes` — целевые долготы для аспектов;
   - `find_longitude_crossings` — поиск пересечений целевой долготы;
   - `refine_crossing` — уточнение времени через бинарный поиск;
   - `get_step_for_planet` — адаптивный шаг сканирования;
   - `find_transit_events` — поиск всех событий транзитов за период.

2. Добавлена функция `jd_to_datetime` в `astro_core/time_service.py`:
   - преобразование Julian Day в datetime UTC.

3. Добавлена функция `get_planet_position` в `astro_core/ephemeris.py`:
   - лёгкий способ получить долготу и скорость одной планеты.

4. Обновлён `run.py`:
   - добавлен флаг `--precise` для режима точных транзитов;
   - добавлена функция `format_transit_events` для читаемого вывода;
   - поддержка точных транзитов в JSON и текстовом формате.

5. Добавлены тесты для точных транзитов:
   - `normalize_diff`;
   - `get_aspect_target_longitudes` для разных аспектов;
   - `jd_to_datetime`;
   - `find_transit_events`;
   - проверка соединения Солнца с натальным Солнцем.

6. Обновлена документация:
   - README.md с примерами точных транзитов;
   - LOG.md с итогами Stage 03.

### Ограничения Stage 03

- Точные транзиты медленнее быстрого режима по дням.
- Для больших периодов (месяц и больше) расчёт может занять значительное время.
- Периоды активности транзитов (вход в орб, пик, выход из орба) пока не рассчитываются.
- Фильтрация по планетам и аспектам пока не реализована.

### Риски, учтённые на этом этапе

- Адаптивный шаг сканирования для разных планет (Луна — 1 час, быстрые — 6 часов, медленные — 1 день).
- Бинарный поиск для уточнения времени пересечения.
- Учёт перехода через 0°/360° через normalize_diff.
- Автоматический учёт ретроградных петель.

---

## Stage 04 — Web Interface (версия 0.5.0)

**Статус:** завершён.

### Что сделано

1. Установлен и подключён Streamlit для веб-интерфейса.

2. Создан `app.py` — веб-интерфейс с возможностью:
   - ввода данных рождения через удобную форму;
   - выбора даты рождения с 1900 года;
   - настройки параметров карты (система домов, Хирон, узлы, Part of Fortune, ASC/MC);
   - расчёта и отображения натальной карты;
   - расчёта и отображения транзитов (быстрый и точный режим);
   - скачивания результатов в формате JSON.

3. Добавлены русскоязычные названия:
   - планет и точек (Солнце, Луна, Меркурий и т.д.);
   - знаков зодиака (Овен, Телец, Близнецы и т.д.);
   - аспектов (Соединение, Секстиль, Квадрат, Трин, Оппозиция);
   - типов объектов (Планета, Угол, Точка);
   - направлений движения (Директное, Ретроградное).

4. Исправлен критический баг:
   - транзиты не находились из-за отсутствия `enabled_aspects` и `orbs` в настройках;
   - исправлено в `find_transits_on_date` и `find_transit_events` через использование значений по умолчанию.

5. Исправлена проблема с отображением секции «Транзиты»:
   - добавлен `st.rerun()` после расчёта натальной карты;
   - секция «Транзиты» теперь появляется сразу после расчёта карты.

6. Добавлен спиннер (`st.spinner`) для индикации процесса расчёта транзитов.

7. Обновлена документация:
   - README.md с информацией о веб-интерфейсе;
   - LOG.md с итогами Stage 04.

### Ограничения Stage 04

- Веб-интерфейс не имеет графического отображения карты (круг с планетами).
- Данные рождения не сохраняются в профили (нужно вводить каждый раз).
- Веб-интерфейс не имеет полноценных тестов (Streamlit сложно тестировать стандартными unit-тестами).
- Фильтрация транзитов по планетам и аспектам пока не реализована.

### Риски, учтённые на этом этапе

- Streamlit может быть медленным при больших объёмах данных.
- Дизайн ограничен возможностями Streamlit.
- Зависимость от Streamlit (можно заменить на другой фреймворк, сохранив `astro_core`).

---

## Stage 05 — Advanced Transits: Periods & Filtering (версия 0.6.0)

**Статус:** завершён.

### Что сделано

1. Добавлены функции поиска периодов активности в `astro_core/transits.py`:
   - `find_orb_boundary` — поиск момента входа/выхода из орба;
   - `refine_orb_boundary` — уточнение момента через бинарный поиск;
   - `find_transit_periods` — поиск всех периодов активности за период.

2. Добавлена фильтрация транзитов:
   - параметр `filter_planets` — фильтрация по транзитным планетам;
   - параметр `filter_aspects` — фильтрация по аспектам;
   - комбинированная фильтрация;
   - поддержка во всех функциях: `find_transit_events`, `find_transit_periods`, `get_transit_calendar`.

3. Константа `DEFAULT_TRANSIT_PLANETS` перемещена из `transits.py` в `constants.py` для архитектурной чистоты.

4. Обновлён веб-интерфейс (`app.py`):
   - добавлен режим «Периоды активности»;
   - добавлены фильтры по планетам (multiselect с русскими названиями);
   - добавлены фильтры по аспектам (multiselect с русскими названиями);
   - отображение периодов активности с колонками: вход в орб, пик, выход из орба;
   - подсказки для каждого режима.

5. Добавлены тесты для новых функций:
   - базовый поиск периодов активности;
   - проверка логики start <= exact <= end;
   - фильтрация по планетам;
   - фильтрация по аспектам;
   - комбинированная фильтрация;
   - фильтрация для периодов и календаря;
   - проверка поведения при пустых фильтрах.

6. Обновлена документация:
   - README.md полностью обновлён (включает все этапы Stage 01–05);
   - LOG.md с итогами Stage 05.

### Ограничения Stage 05

- Режим «Периоды активности» медленнее других режимов.
- Для очень медленных планет с большим орбом период активности может быть несколько месяцев (ограничено параметром max_days=60).
- Фильтрация по натальным точкам пока не реализована.

### Риски, учтённые на этом этапе

- Фильтрация применяется до расчёта для улучшения производительности.
- Бинарный поиск для уточнения границ орба.
- Параметр max_days ограничивает время поиска границ.
- Для ретроградных петель каждый проход через аспект — отдельный период.

---

## Stage 06 — Birth Profiles (версия 0.7.0)

**Статус:** завершён.

### Что сделано

1. Создан модуль `astro_core/profiles.py`:
   - `save_profile` — сохранение профиля в JSON-файл;
   - `load_profile` — загрузка профиля по имени;
   - `list_profiles` — список всех профилей с сортировкой по имени;
   - `delete_profile` — удаление профиля;
   - `profile_to_filename` — безопасное преобразование имени в имя файла;
   - автоматическое добавление метаданных (`created_at`, `updated_at`);
   - пропуск повреждённых файлов без блокировки работы.

2. Обновлён веб-интерфейс (`app.py`):
   - секция «Профили» в верхней части боковой панели;
   - выпадающий список всех сохранённых профилей;
   - кнопка «📂 Загрузить» для заполнения формы данными профиля;
   - кнопка «💾 Сохранить как профиль» для сохранения текущих данных;
   - кнопка «🗑️ Удалить» с явным подтверждением (чекбокс);
   - уведомление о перезаписи существующего профиля;
   - автоматическое обновление списка профилей после операций.

3. Архитектурные решения:
   - профили хранятся в папке `profiles/` как отдельные JSON-файлы;
   - один профиль = один файл (простота, надёжность, прозрачность);
   - имена файлов генерируются из имени профиля с санитизацией;
   - поддержка кириллических имён профилей;
   - разделение ответственности: модуль `profiles.py` не знает про веб-интерфейс.

4. Добавлены тесты для модуля профилей (`tests/test_profiles.py`):
   - преобразование имени в имя файла;
   - сохранение и загрузка профиля;
   - обработка отсутствующих обязательных полей;
   - обработка несуществующих профилей;
   - обновление метаданных при повторном сохранении;
   - сохранение дополнительных полей;
   - список профилей с сортировкой;
   - пропуск повреждённых файлов;
   - удаление профилей.

5. Обновлена документация:
   - README.md полностью обновлён (включает все этапы Stage 01–06);
   - LOG.md с итогами Stage 06.

### Ограничения Stage 06

- Нет редактирования имени профиля (только удаление и создание нового).
- Нет экспорта/импорта всех профилей сразу (резервное копирование через файловую систему).
- Перезапись профиля не требует подтверждения (показывает уведомление постфактум).
- Нет поиска по профилям (для нескольких десятков профилей не требуется).

### Риски, учтённые на этом этапе

- Конфликт имён: сохранение профиля с существующим именем перезаписывает его. Пользователь уведомляется о перезаписи.
- Повреждение файлов: повреждённые файлы пропускаются при выводе списка без блокировки работы.
- Случайное удаление: удаление требует явного подтверждения через чекбокс.
- Резервное копирование: достаточно скопировать папку `profiles/`.

---

## Этап 07 — Графическое отображение карты (версия 0.8.0)

**Статус:** завершён.

### Что сделано

1. Создан модуль `astro_core/chart_svg.py`:
   - отображение натальной карты в векторном формате SVG;
   - классическая астрологическая карта (круг знаков, дома, планеты, аспекты, ASC/MC);
   - три режима подписей: только символы, только слова, символы + слова;
   - астрологические символы Юникода для планет и знаков зодиака;
   - масштабирование карты (зум);
   - перемещение видимой области (панорамирование);
   - изменение размера карты.

2. Архитектурное решение:
   - выбран SVG вместо Matplotlib для поддержки будущей интерактивности;
   - зум и смещение реализованы через изменение атрибута `viewBox`;
   - режим «только символы» установлен по умолчанию;
   - размер символов планет увеличен для читаемости.

3. Обновлён веб-интерфейс (`app.py`):
   - добавлена вкладка «🌌 Карта» (первая в списке);
   - элементы управления: чекбоксы, ползунки зума и смещения;
   - чекбокс «Показать названия словами» для режима символов;
   - кнопка «📥 Скачать карту (SVG)».

4. Добавлены тесты для модуля отображения (`tests/test_chart_svg.py`):
   - корректность преобразования координат;
   - валидность генерируемого SVG;
   - режимы подписей;
   - зум и смещение;
   - устойчивость к пустым данным.

5. Обновлена документация:
   - README полностью обновлён (включает все этапы 01–07);
   - LOG с итогами Этапа 07.

### Ограничения Этапа 07

- Графическая карта не имеет интерактивности мышью (управление через ползунки).
- Нет анимации при изменении зума или смещения.
- Подписи планет могут накладываться при близком расположении планет.
- Нет графического отображения транзитных карт (только натальные).

### Риски, учтённые на этом этапе

- Поддержка символов Юникода зависит от шрифтов в системе.
- Наложение подписей при близком расположении планет частично решено смещением по радиусу.
- Зум реализован через `viewBox`, что является стандартным механизмом SVG.

---

## Этап 08 — Интерактивность, города, сидерический зодиак (версия 0.8.0)

**Статус:** завершён.

### Что сделано

1. **Интерактивная карта:**
   - зум колесом мыши относительно позиции курсора;
   - перетаскивание карты;
   - кнопка сброса вида;
   - реализовано через встроенный JavaScript в SVG без внешних зависимостей;
   - модуль `chart_interactive.py`.

2. **Выбор города для натальной карты:**
   - база городов в `data/cities.json` (~100 городов: Россия, СНГ, Европа, мировые центры);
   - поиск города по названию (на русском и английском);
   - автоподстановка координат и часового пояса;
   - модуль `cities.py`;
   - ручной ввод координат сохранён как запасной вариант.

3. **Автоматическое определение часового пояса и UTC offset:**
   - определение по часовому поясу города и дате рождения;
   - учёт летнего времени для исторических дат;
   - модуль `timezone_service.py`;
   - библиотека `zoneinfo` + пакет `tzdata`.

4. **Город для транзитной карты:**
   - выбор города транзита;
   - отображение домов транзита во внешнем кольце;
   - отображение углов транзита (Тр. ASC, Тр. MC);
   - дома и углы рассчитываются через `build_natal_chart` для города транзита.

5. **Графическая транзитная карта:**
   - натальные планеты во внутреннем круге;
   - транзитные планеты во внешнем круге;
   - натальные аспекты внутри внутреннего круга;
   - транзитные аспекты пунктирными линиями между кругами;
   - согласованность фильтров между таблицами и картой;
   - согласованность чекбокса «Показать аспекты».

6. **Сидерический зодиак:**
   - поддержка тропического и сидерического зодиака;
   - выбор системы аянамши: Лахири, Раман, Кришнамурти, Фаган-Брэдли;
   - корректный расчёт позиций планет в сидерическом зодиаке;
   - поддержка сидерического зодиака в транзитах;
   - элементы выбора в веб-интерфейсе;
   - константы `AYANAMSHA_SYSTEMS`, `ZODIAC_TYPES` в `constants.py`;
   - функция `setup_zodiac_mode` в `ephemeris.py`.

7. **Редактирование профилей:**
   - кнопка «✏️ Редактировать» для загрузки профиля в форму;
   - сохранение изменений;
   - переименование профиля (старая запись удаляется, создаётся новая);
   - отмена редактирования;
   - пометка «✏️ Редактируется: …» в режиме редактирования.

8. **Тесты:**
   - тесты для сидерического зодиака (`test_sidereal.py`);
   - расширенные тесты для профилей (`test_profiles_extended.py`);
   - тесты для графического отображения (`test_chart_svg.py`).

9. **Документация:**
   - обновлён README с информацией об Этапе 08;
   - обновлён LOG с итогами Этапа 08.

### Ограничения Этапа 08

- Интерактивность работает только в браузере (не в скачанном SVG).
- База городов ограничена (~100 городов).
- Дома транзита рассчитаны на полдень выбранной даты.
- Профили не сохраняют тип зодиака и систему аянамши.

## Этап 09 — Прогрессии (версия 0.9.0)

**Статус:** завершён.

### Что сделано

1. **Модуль `astro_core/progressions.py`:**
   - `calculate_secondary_progressions` — вторичные прогрессии (день за год);
   - `calculate_solar_arc_progressions` — прогрессии солнечной дуги;
   - `find_progression_aspects` — аспекты между прогрессивными и натальными точками.

2. **Вторичные прогрессии:**
   - принцип: 1 день после рождения = 1 год жизни;
   - расчёт прогрессивных планет, домов и углов;
   - корректное вычисление возраста и прогрессивной даты;
   - поддержка сидерического зодиака.

3. **Прогрессии солнечной дуги:**
   - вычисление солнечной дуги (разница между прогрессивным и натальным Солнцем);
   - продвижение всех натальных планет на величину солнечной дуги;
   - аспекты прогрессивных планет солнечной дуги к натальным точкам.

4. **Интеграция в веб-интерфейс:**
   - новая вкладка «🔄 Прогрессии»;
   - выбор даты прогрессии;
   - выбор типа прогрессий (вторичные / солнечная дуга);
   - таблица прогрессивных планет;
   - таблица прогрессивных точек (ASC, MC, Part of Fortune);
   - таблица аспектов прогрессий к натальным точкам;
   - кнопка скачивания результата в формате JSON.

5. **Тесты:**
   - базовый расчёт вторичных прогрессий;
   - прогрессии на дату рождения равны натальной карте;
   - прогрессивное Солнце движется примерно на 1° в год;
   - базовый расчёт прогрессий солнечной дуги;
   - солнечная дуга положительна и разумна;
   - все планеты продвигаются на одинаковую величину;
   - аспекты прогрессий к натальным точкам.

6. **Документация:**
   - обновлён README с информацией об Этапе 09;
   - обновлён LOG с итогами Этапа 09.

### Ограничения Этапа 09

- Прогрессии отображаются только в таблицах (нет графического отображения прогрессивной карты).
- Дома прогрессий рассчитываются, но не отображаются в интерфейсе.
- Нет поддержки минорных и третичных прогрессий.

### Риски, учтённые на этом этапе

- Корректное вычисление прогрессивной даты (возраст в годах добавляется как количество дней).
- Поддержка сидерического зодиака через передачу настроек.
- Обработка ошибок при расчёте прогрессий.

---

## Этап 10 — Дальнейшие улучшения (план)

**Статус:** следующий этап.

### Возможные направления

1. **Графическое отображение прогрессивных карт.**
2. **Фильтрация транзитов и прогрессий по натальным точкам.**
3. **Расширение базы городов.**
4. **Улучшения UX** (история расчётов, сохранение настроек, тёмная тема).
5. **Минорные и третичные прогрессии.**
6. **Сравнение карт (синастрия).**

Решение о следующем этапе будет принято после обсуждения с владельцем проекта.

### Риски, учтённые на этом этапе

- Интерактивность реализована через встроенный JavaScript без внешних зависимостей.
- База городов хранится локально, работает без интернета.
- Сидерический зодиак использует стандартные аянамши из Swiss Ephemeris.
- Сброс режима сидерического зодиака при переключении на тропический.
- Согласованность фильтров между таблицами транзитов и графической картой.

### Риски, учтённые на этом этапе

- Интерактивность реализована через встроенный JavaScript, без внешних зависимостей.
- База городов хранится локально, работает без интернета.
- Сидерический зодиак использует стандартные ayanamsha из Swiss Ephemeris.

### Возможные направления

1. **Прогрессии** (вторичные прогрессии, solar arc).
2. **Фильтрация по натальным точкам** (например, показать только транзиты к Солнцу).
3. **Интерактивность графической карты** (управление мышью, анимация).
4. **Графическое отображение транзитных карт**.
5. **Улучшения UX** (история расчётов, сохранение настроек).

Решение о следующем этапе будет принято после обсуждения с владельцем проекта.
